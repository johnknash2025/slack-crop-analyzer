{
  "name": "slack-crop-analyzer",
  "version": "1.0.0",
  "description": "Slack bot for analyzing crop images using Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20231218.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}